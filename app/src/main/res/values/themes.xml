<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SchoolConnect" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.SchoolConnect" parent="Base.Theme.SchoolConnect" />

    <style name="FigmaRadioButton" parent="Widget.MaterialComponents.CompoundButton.RadioButton">
        <!-- Circle color -->
        <item name="buttonTint">@color/radio_selector</item>

        <!-- Text -->
        <item name="android:textColor">#1E1E2F</item>
        <item name="android:textSize">18sp</item>

        <!-- Layout -->
        <item name="android:layoutDirection">rtl</item>
        <item name="android:paddingStart">16dp</item>
        <item name="android:paddingEnd">16dp</item>
    </style>

    <style name="FilterChipStyle" parent="Widget.MaterialComponents.Chip.Choice">
        <item name="chipStrokeWidth">1.2dp</item>
        <item name="chipStrokeColor">@color/chip_stroke_selector</item>
        <item name="chipBackgroundColor">@color/chip_bg_selector</item>
        <item name="android:textColor">@color/chip_text_selector</item>
        <item name="chipCornerRadius">20dp</item>
        <item name="android:minHeight">36dp</item>
    </style>


</resources>